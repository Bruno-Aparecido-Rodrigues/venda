server:
  port: 8081

spring:
  application:
    name: venda
  datasource:
    url: jdbc:h2:mem:venda
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true